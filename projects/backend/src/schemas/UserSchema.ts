import { isString, validateEmail } from "../utils/validations.js";
import { escapeChar } from "../utils/utils.js";

export const validateUserRegister = (userRegisterInput) => {

}

export const validateUserLogin = (userLoginInput) => {
    
}